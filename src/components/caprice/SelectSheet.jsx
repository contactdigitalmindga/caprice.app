import React,{useState}from'react';import{Check,ChevronDown}from'lucide-react';import{Sheet,SheetContent,SheetHeader,SheetTitle}from'@/components/ui/sheet';
export default function SelectSheet({value,options,onChange,label,placeholder='Sélectionner'}){const[open,setOpen]=useState(false);return <><button type="button" className="select-trigger" onClick={()=>setOpen(true)}><span>{value||placeholder}</span><ChevronDown size={16}/></button><Sheet open={open} onOpenChange={setOpen}><SheetContent side="bottom" className="select-sheet"><SheetHeader><SheetTitle>{label||placeholder}</SheetTitle></SheetHeader><div className="select-options">{options.map(o=><button key={o} className={value===o?'selected':''} onClick={()=>{onChange(o);setOpen(false)}}><span>{o}</span>{value===o&&<Check size={18}/>}</button>)}</div></SheetContent></Sheet></>;}