import React from 'react';import{Bell,ShoppingBag}from'lucide-react';import{useCaprice}from'./AppContext';
export default function TopBar(){const{setScreen,cart}=useCaprice();return <header className="flex items-center justify-between px-5 pt-7 pb-3"><div><p className="text-xs text-stone-500">Bonjour 👋</p><h2 className="font-display text-xl">Bienvenue chez Caprice</h2></div><div className="flex gap-2"><button className="icon" aria-label="Notifications" onClick={()=>setScreen('notifications')}><Bell size={18}/></button><button className="icon relative" aria-label="Panier" onClick={()=>setScreen('cart')}><ShoppingBag size={18}/>{cart.length>0&&<b className="badge">{cart.reduce((a,v)=>a+v.q,0)}</b>}</button></div></header>}