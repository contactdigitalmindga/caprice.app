import React from'react';import{motion}from'framer-motion';import{Home,UtensilsCrossed,CalendarDays,ReceiptText,UserRound}from'lucide-react';import{useCaprice}from'./AppContext';
const n=[['home','Accueil',Home],['menu','Menu',UtensilsCrossed],['reserve','Réserver',CalendarDays],['orders','Commandes',ReceiptText],['profile','Profil',UserRound]];
export default function BottomNav(){const{tab,switchTab}=useCaprice();return <nav className="bottom-nav">{n.map(([id,l,I])=>{const a=tab===id;return <motion.button key={id} whileTap={{scale:.82}} transition={{type:'spring',stiffness:500,damping:18}} onClick={()=>switchTab(id)} className={a?'active':''}>{a&&<motion.span layoutId="nav-pill" className="nav-pill" transition={{type:'spring',stiffness:420,damping:30}}/>}<motion.span className="nav-icon" animate={{y:a?-3:0,scale:a?1.15:1}} transition={{type:'spring',stiffness:400,damping:18}}><I size={20}/></motion.span><span className="nav-label">{l}</span></motion.button>})}</nav>;}